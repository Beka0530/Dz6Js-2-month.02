obj = {
    firstName: 'Напишите свое имя',
    lastName: 'Напишите свою фамилию',
    was_born: 'Год рождения',
    password: '123',
};

function getFullName(firstName, lastName) {
    return obj.fullName = `${firstName} ${lastName}`;
}

getFullName(obj.firstName, obj.lastName);

console.log('', obj);

